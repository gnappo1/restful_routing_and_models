<%= display_h1_conditionally(@brand) %>

<%= form_with(url: products_path, method: :get) do |f| %>
    <%= f.label :search, "Search products by name" %>
    <%= f.text_field :search %>
    <%= f.submit "Search" %>
<% end %>

<%= display_list_conditionally(@products) %>
