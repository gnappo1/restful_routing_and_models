<h1>Brands</h1>

<% if @brands.length > 0 %>
    <ul>
        <% @brands.each do |brand|%>
            <li><%= link_to brand.name, brand %></li>
        <% end %>
    </ul>
<% else %>
    <h2>There are currently no brands! Create one <%= link_to "here", new_brand_path %></h2>
<% end %>